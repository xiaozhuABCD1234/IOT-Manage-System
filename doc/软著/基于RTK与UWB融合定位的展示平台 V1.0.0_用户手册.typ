// ---------- 可调参数 ----------
#let soft-name = "基于RTK与UWB融合定位的展示平台"
#let soft-ver = "V1.0.0"
#let doc-type = "用户手册"

#set heading(numbering: "1.1.1")

// ---------- 页眉函数 ----------
#let soft-header() = context {
  set text(size: 10.5pt, font: ("SimSun", "Songti SC", "serif"))
  smallcaps(soft-name) + h(1fr) + str(counter(page).at(here()).first())
  v(2pt, weak: true)
  line(length: 100%, stroke: 0.25pt)
}

// ---------- 封面 ----------
#let cover() = {
  align(center)[
    #text(size: 20pt, weight: "bold", soft-name + " " + soft-ver) \
    #v(3em)
    #place(center + horizon, text(size: 20pt, doc-type))
  ]
  pagebreak()
}

// ---------- 全局设置 ----------
#set page(
  margin: (top: 2.8cm, bottom: 2.5cm, x: 2cm),
  header: soft-header(),
  numbering: none,
)
#set par(justify: true)
#cover()

#outline(title: "目录")
#pagebreak()
#counter(page).update(1)
#set page(
  margin: (top: 2.8cm, bottom: 2.5cm, x: 2cm),
  header: soft-header(),
  numbering: none,
)

#set text(lang: "zh", region: "cn")
#set figure(numbering: "1")

= 系统概述

== 展示平台简介

展示平台是 IOT 管理系统的监控查看端，面向需要实时监控设备状态、查看位置信息的一线操作人员。

本系统是基于 RTK（实时动态载波相位差分技术）与 UWB（超宽带）融合定位技术的可视化展示平台。通过实时精确的位置跟踪，可以有效监控施工人员进入危险区域、设备碰撞预警、作业边界管控等，显著提升施工安全水平。

== 主要功能

#table(
  columns: (auto, 1fr),
  align: left,
  [*功能*], [*说明*],
  [实时地图], [在地图上查看设备当前位置，支持 RTK（室外）和 UWB（室内）两种定位模式],
  [地图总览], [综合展示所有设备的实时位置],
  [历史轨迹], [查看设备历史移动轨迹，支持时间筛选与回放],
  [设备状态], [查看设备在线/离线状态，支持筛选与搜索],
  [警报查看], [查看设备触发的警报信息，支持处理与限流],
  [电子围栏], [可视化围栏管理与实时检测],
  [距离监控], [设备间安全距离设置与实时预警],
)

== 系统架构

展示平台采用前后端分离架构，前端基于 Vue 3 + TypeScript 构建，通过 API 网关与后端微服务通信。

#figure(image("assets/image-10.png"), caption: [总体架构设计])

=== 软件功能模块结构图

#figure(image("assets/展示平台功能模块结构图.png"), caption: [展示平台功能模块结构图])

*模块说明：*

#table(
  columns: (auto, 1fr, 1fr),
  align: left,
  [*模块名称*], [*功能描述*], [*输入/输出*],
  [用户认证模块], [处理用户登录、Token管理、权限验证], [输入：用户名/密码；输出：JWT Token/用户信息],
  [地图展示模块], [高德地图集成、UWB自定义地图渲染、设备标记显示], [输入：设备坐标数据；输出：可视化地图界面],
  [实时通信模块], [MQTT WebSocket连接、消息订阅、数据推送], [输入：MQTT消息；输出：实时位置更新],
  [历史轨迹模块], [轨迹数据查询、时间筛选、轨迹回放控制], [输入：时间范围/设备ID；输出：轨迹路径数据],
  [设备监控模块], [设备状态列表、筛选搜索、在线离线检测], [输入：设备状态数据；输出：状态监控界面],
  [警报处理模块], [警报接收、分类显示、处理状态更新、限流控制], [输入：警报消息；输出：警报列表/处理结果],
  [围栏管理模块], [围栏可视化、创建编辑、实时检测显示], [输入：围栏配置/位置数据；输出：围栏状态/警报],
  [距离监控模块], [安全距离配置、实时距离计算、预警显示], [输入：距离配置/位置数据；输出：距离警报],
)

== 技术架构

#table(
  columns: (auto, 1fr),
  align: left,
  [*类别*], [*技术栈*],
  [前端框架], [Vue 3.5.18 + TypeScript 5.8 + Vite],
  [UI组件库], [Radix Vue + Tailwind CSS 4.1],
  [地图服务], [高德地图API + 自定义Canvas绘制],
  [实时通信], [MQTT.js 5.14+（WebSocket）],
  [状态管理], [Pinia 3.0+],
  [路由管理], [Vue Router 4.5+],
  [HTTP客户端], [Axios 1.11+],
  [构建工具], [Vite 6.0+],
  [代码规范], [ESLint + Prettier],
)

= 快速开始

== 环境要求

- 操作系统：Windows 10/11、macOS 10.15+、Linux Ubuntu 18.04+
- 浏览器：Chrome 90+、Firefox 88+、Edge 90+、Safari 14+
- 网络：与服务器正常通信（内网或互联网）
- 分辨率：推荐 1920×1080 及以上

== 访问系统

*操作步骤：*

1. 打开浏览器（以Chrome为例）
2. 在地址栏输入系统地址：`http://localhost` 或 `http://服务器IP`
3. 按回车键，等待页面加载
4. 系统加载完成后，自动跳转至登录页面

== 登录

=== 登录操作流程

*步骤1：进入登录页面*

系统加载完成后，显示登录界面，包含：
- 左侧：系统宣传图片（施工现场场景）
- 右侧：登录表单区域

#figure(image("assets/登录界面.png", height: 8cm), caption: [登录页面完整界面]) <登录页面>

*步骤2：输入用户名*

1. 点击"用户名"输入框（或按Tab键聚焦）
2. 输入用户名（如：admin）
3. 输入框显示已输入内容


*步骤3：输入密码*

1. 点击"密码"输入框
2. 输入密码（如：admin），密码显示为圆点掩码
3. 如需查看明文，可点击输入框右侧眼睛图标（如提供该功能）


*步骤4：点击登录*

1. 确认用户名和密码已正确输入
2. 点击黑色"登录"按钮
3. 按钮显示加载状态（转圈动画）
4. 登录成功后自动跳转至首页


*步骤5：登录成功*

登录成功后，系统跳转至地图总览页面，显示：
- 顶部导航栏（展示平台Logo、菜单、用户信息）
- 左侧：RTK实时地图（室外设备位置）
- 右侧：UWB实时地图（室内设备位置）


=== 默认账户

#table(
  columns: (auto, auto, auto),
  align: center,
  [*角色*], [*用户名*], [*密码*],
  [管理员], [admin], [admin],
)

=== 退出登录

*操作步骤：*

1. 将鼠标移至页面右上角用户名区域（显示"admin"或当前用户名）
2. 点击用户名，弹出下拉菜单
3. 选择"退出登录"选项
4. 系统清除登录状态，跳转回登录页面


= 界面导航

== 主界面布局说明

登录成功后进入系统主界面，整体布局分为三个区域：

#figure(image("assets/页面结构.png"), caption: [主界面布局分区说明])

*区域说明：*

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*区域编号*], [*区域名称*], [*功能说明*],
  [①], [顶部导航栏], [系统Logo、主导航菜单（地图、电子标记、关于）、用户信息、主题切换],
  [③], [主内容区], [地图显示区域（RTK地图/UWB地图/地图总览）],
  [④], [右侧信息面板], [围栏列表、警报列表、选中设备详情（可选）],
  [⑤], [底部状态栏], [系统状态、连接状态、数据更新时间],
)

== 顶部导航菜单

#figure(image("/assets/image-3.png"), caption: [顶部导航菜单]) <顶部导航菜单>

*菜单结构：*

#table(
  columns: (auto, 1fr, 1fr),
  align: left,
  [*菜单名称*], [*子菜单项*], [*功能说明*],
  [地图], [地图总览], [同时显示RTK和UWB设备位置，适合指挥调度],
  [], [实时RTK地图], [高德地图显示室外RTK定位设备],
  [], [实时UWB地图], [自定义平面图显示室内UWB定位设备],
  [], [历史轨迹], [查看设备历史移动路径，支持时间筛选],
  [], [UWB基站管理], [查看和管理UWB基站位置],
  [], [地图设置], [查看已配置的地图列表],
  [电子标记], [标记状态], [监控所有设备在线/离线状态],
  [], [标记管理], [设备增删改查、类型和标签管理],
  [], [距离设置], [配置设备间安全距离],
  [关于], [系统信息], [查看系统版本、版权信息],
  [], [个人中心], [修改密码、查看个人信息],
)

== 菜单展开操作演示

*步骤1：鼠标悬停或点击"地图"菜单*

1. 将鼠标移至顶部"地图"菜单项
2. 菜单下方弹出下拉面板，显示子菜单选项
3. 每个子菜单项包含图标、名称和简短说明

#figure(image("assets/地图子菜单.png"), caption: [地图菜单展开效果])

*步骤2：选择子菜单*

1. 移动鼠标至目标子菜单（如"实时RTK地图"）
2. 菜单项高亮显示
3. 点击鼠标左键，进入对应功能页面


= 实时地图

实时地图用于在地图上查看设备的当前位置，支持室外RTK定位和室内UWB定位两种模式。

== RTK 实时地图

=== 功能说明

RTK（实时动态定位）是一种高精度GPS定位技术，适用于室外环境，定位精度可达厘米级。

*核心功能：*
- 在高德地图上显示设备实时位置
- 支持多设备同时显示（最多500个设备）
- 显示设备最新坐标（经纬度，WGS84坐标系）
- 实时更新设备位置（默认1秒刷新）
- 设备标记颜色标识状态（绿色=正常，红色=警报）
- 点击标记查看设备详细信息

=== 进入RTK实时地图

*操作步骤：*

1. 点击顶部导航栏"地图"菜单
2. 在下拉菜单中选择"实时RTK地图"
3. 页面加载高德地图，显示当前区域
4. 在线设备以标记形式显示在地图上


=== 地图界面元素说明

#figure(image("assets/室外RTK地图.png"), caption: [RTK实时地图页面完整界面]) <室外RTK地图>

*界面元素说明：*

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*编号*], [*元素名称*], [*功能说明*],
  [①], [地图控制栏], [缩放按钮（+/-）、地图类型切换（卫星/标准）、定位当前位置],
  [②], [设备标记], [绿色圆点=设备在线正常，红色圆点=设备触发警报，带脉冲动画],
  [③], [设备信息弹窗], [点击标记后弹出，显示设备名称、ID、坐标、状态、最后更新时间],
  [④], [设备列表面板], [左侧显示所有设备列表，支持筛选和搜索],
  [⑤], [围栏显示], [蓝色多边形表示已配置的电子围栏区域],
  [⑥], [图例说明], [右下角显示标记颜色含义说明],
)

=== 查看设备详情操作

*步骤1：定位设备*

1. 在左侧设备列表中找到目标设备
2. 或直接在地图上查找设备标记

*步骤2：点击设备标记*

1. 将鼠标移至地图上的设备标记（绿色/红色圆点）
2. 鼠标指针变为手型，表示可点击
3. 单击鼠标左键

#figure(image("assets/电子标记子菜单.png"), caption: [步骤2：点击设备标记])

*步骤3：查看详情*

1. 标记上方弹出信息卡片
2. 显示内容：
  - 设备名称（如"吊车-001"）
  - 设备ID（如"device-001"）
  - 设备类型（如"吊车"）
  - 当前坐标（纬度, 经度）
  - 在线状态（在线/离线）
  - 最后更新时间（2026-04-11 14:32:15）
  - 当前速度（如移动中）
3. 点击卡片外区域或关闭按钮，关闭弹窗

#figure(image("/assets/image-5.png"), caption: [步骤3：查看设备详细信息])

=== 地图缩放与移动操作

*缩放操作：*
1. 点击地图右上角"+"按钮，放大地图（显示更多细节）
2. 点击"-"按钮，缩小地图（显示更大范围）
3. 或使用鼠标滚轮：向上滚动放大，向下滚动缩小

*移动操作：*
1. 按住鼠标左键不放，拖动地图
2. 地图随鼠标移动，显示不同区域

#figure(image("/assets/image-6.png"), caption: [地图缩放与移动操作])

== UWB 实时地图

=== 功能说明

UWB（超宽带）是一种室内定位技术，适用于室内环境，定位精度可达10-30厘米。

*核心功能：*
- 在自定义地图（平面图）上显示设备位置
- 室内定位精度高（分米级）
- 支持多设备同时显示
- 实时更新设备位置
- 显示UWB基站位置（蓝色三角形标记）
- 支持楼层切换（多楼层场景）

=== 进入UWB实时地图

*操作步骤：*

1. 点击顶部导航栏"地图"菜单
2. 在下拉菜单中选择"实时UWB地图"
3. 页面加载已配置的自定义地图（平面图）
4. 显示设备坐标点和基站位置


=== 地图界面元素说明

#figure(image("assets/室内UWB地图.png"), caption: [UWB实时地图页面完整界面]) <室内UWB地图>

*界面元素说明：*

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*编号*], [*元素名称*], [*功能说明*],
  [①], [坐标网格], [显示X/Y坐标网格，方便定位，单位：厘米],
  [②], [建筑轮廓], [黑色线条表示墙壁、房间边界],
  [③], [UWB基站], [蓝色三角形标记，标注A基站、B基站等],
  [④], [设备位置], [橙色圆点表示设备当前位置，带设备名称标签],
  [⑤], [电子围栏], [绿色半透明区域表示安全围栏，红色表示危险区域],
  [⑥], [坐标标尺], [左下角显示比例尺（如1000单位=10米）],
)

=== 查看设备轨迹（实时）

*操作步骤：*

1. 在UWB地图页面，点击左侧设备列表中的设备
2. 勾选"显示轨迹"复选框
3. 地图显示该设备最近30秒的运动轨迹（虚线连接）
4. 轨迹点按时间顺序连接，显示运动方向

#figure(image("/assets/image-7.png"), caption: [UWB实时轨迹显示])

== 地图总览

=== 功能说明

综合展示页面，同时查看RTK和UWB设备的实时位置，适合指挥调度场景。

*核心功能：*
- 左右分屏显示：左侧RTK地图，右侧UWB地图
- 同时显示室内外所有设备
- 快速了解整体监控状态
- 支持全屏切换（双击单侧地图）

=== 进入地图总览

*操作步骤：*

1. 点击顶部导航栏"地图"菜单
2. 选择"地图总览"
3. 页面加载双地图视图

#figure(image("/assets/image-4.png"), caption: [地图总览页面]) <地图总览>

= 设备状态监控

设备状态监控页面用于查看所有设备的在线/离线状态，支持筛选和搜索。

== 标记状态页面

=== 进入标记状态页面

*操作步骤：*

1. 点击顶部导航栏"电子标记"菜单
2. 选择"标记状态"
3. 进入设备状态监控页面


=== 页面布局说明

#figure(image("assets/设备状态监控.png"), caption: [设备标记状态页面]) <设备状态监控>

*页面分区：*

#table(
  columns: (auto, 1fr, 1fr),
  align: left,
  [*区域*], [*内容*], [*功能*],
  [左侧面板], [标记在线状态统计], [显示在线/离线设备数量，实时刷新],
  [右侧上], [类型列表], [显示所有设备类型及默认危险半径],
  [右侧下], [分组列表], [显示设备分组情况],
  [底部], [设备详情列表], [分页显示设备详细信息],
)

=== 查看设备状态详情

*显示信息字段：*

#table(
  columns: (auto, 1fr),
  align: left,
  [*字段*], [*说明*],
  [设备名称], [设备显示名称（可自定义）],
  [设备类型], [设备所属分类（如人员、车辆、设备）],
  [在线状态], [在线（绿色圆点）/离线（灰色圆点）],
  [最后在线时间], [最近一次收到数据的时间（精确到秒）],
  [最后位置], [设备最后已知位置（地图坐标或"未知"）],
  [危险半径], [该设备的安全距离设置],
  [分组], [设备所属分组标签],
)

== 状态说明

#table(
  columns: (auto, 1fr, auto),
  align: left,
  [*状态*], [*说明*], [*标识*],
  [在线], [设备当前正在发送数据，5秒内收到过心跳], [绿色圆点+脉冲动画],
  [离线], [设备超过30秒未发送数据，可能断电或网络故障], [灰色圆点],
  [警报], [设备触发距离警报或围栏警报], [红色圆点+闪烁],
  [未激活], [设备已注册但未上报过数据], [黄色圆点],
)

== 筛选功能操作

=== 按设备类型筛选

*操作步骤：*

+ 在类型列表页面，点击列出对映的设备类型
+ 设备列表自动刷新，仅显示该类型的设备

#figure(image("/assets/image-8.png"), caption: [按设备类型筛选操作])

=== 按分组筛选

*操作步骤：*

+ 在分组列表页面，点击列出对映的设备分组
+ 设备列表自动刷新，仅显示该分组的设备

#figure(image("/assets/image-9.png"), caption: [按标签筛选设备])

=== 按在线状态筛选

*操作步骤：*

1. 点击"在线状态"单选按钮组
2. 选择"全部"（默认）、"仅在线"或"仅离线"
3. 设备列表实时更新显示结果

== 状态刷新机制

- *自动刷新*：页面每5秒自动轮询设备状态
- *手动刷新*：点击右上角刷新按钮，立即更新状态
- *实时推送*：通过MQTT WebSocket，设备状态变更时实时更新（延迟< 1秒）

== 设备标记管理

提供设备的CRUD（创建、读取、更新、删除）功能。

#figure(image("assets/设备标记管理.png"), caption: [设备标记管理页面]) <设备标记管理>

=== 创建设备完整流程

*步骤1：打开创建对话框*

1. 在标记管理页面，点击右上角"新建标记"按钮
2. 弹出"新建标记"对话框（模态窗口）

#figure(image("assets/创建标记.png", height: 10cm), caption: [创建设备对话框]) <创建设备>

*步骤2：选择设备ID*

1. 点击"设备ID"下拉框
2. 系统显示所有未分配标记的设备ID列表
3. 选择目标设备ID（如"device-001"）
4. 如列表为空，表示所有设备已分配标记

// #figure(image("assets/创建设备步骤2选择ID.png"), caption: [步骤2：选择设备ID])

*步骤3：输入标记名称*

1. 在"标记名称"输入框中输入显示名称
2. 建议使用易识别名称（如"吊车-001"或"张三-安全帽"）
3. 名称长度限制：2-50个字符

*步骤4：选择标记类型*

1. 点击"标记类型"下拉框
2. 选择预定义类型（如"默认"、"吊车"、"人员"）
3. 不同类型关联不同的默认危险半径

// #figure(image("assets/创建设备步骤4选择类型.png"), caption: [步骤4：选择标记类型])

*步骤5：设置危险半径（可选）*

1. "危险半径"输入框留空，表示使用类型默认值
2. 如需自定义，输入数值（单位：米，如"5"表示5米）
3. 输入-1表示不启用距离检测

*步骤6：添加分组标签*

1. 在"分组"输入框中输入分组名称
2. 按Enter键添加标签
3. 可添加多个分组标签
4. 点击标签右侧×可移除

// #figure(image("assets/创建设备步骤6添加分组.png"), caption: [步骤6：添加分组标签])

*步骤7：设置历史轨迹保存*

1. 勾选"保存历史轨迹"开关（默认开启）
2. 开启后，该设备的位置数据将持久化存储到MongoDB
3. 关闭则仅保留实时位置，不保存历史

*步骤8：保存*

1. 确认所有信息填写正确
2. 点击"创建"按钮
3. 系统验证数据，创建标记
4. 成功提示：右上角显示"创建成功"通知
5. 对话框关闭，设备列表自动刷新

// #figure(image("assets/创建设备成功提示.png"), caption: [步骤8：创建成功提示])

=== 编辑设备流程

*步骤1：选择设备*

1. 在设备列表中找到目标设备
2. 点击该行右侧的"编辑"按钮（铅笔图标）

#figure(image("assets/修改标记.png", height: 10cm), caption: [修改设备对话框]) <修改设备>

*步骤2：修改信息*

1. 在对话框中修改需要变更的字段
2. 可修改：标记名称、类型、危险半径、分组、历史保存设置
3. 设备ID不可修改（灰色显示）

*步骤3：保存*

1. 点击"修改"按钮
2. 系统更新设备信息
3. 显示"修改成功"提示
4. 列表自动刷新显示最新信息

=== 删除设备流程

*步骤1：选择设备*

1. 在设备列表中找到目标设备
2. 点击该行右侧的"删除"按钮（垃圾桶图标）

*步骤2：确认删除*

1. 弹出确认对话框："确定要删除标记'XXX'吗？此操作不可撤销。"
2. 仔细核对设备名称，避免误删

#figure(image("/assets/image-10.png"), caption: [删除确认对话框])

*步骤3：执行删除*

1. 点击"确定"按钮，确认删除
2. 或点击"取消"，放弃操作
3. 删除成功后，显示提示通知
4. 设备从列表中移除
= 警报机制

当设备触发警报条件时，系统通过 MQTT 协议向对应的电子标签下发指令，触发标签的震动和蜂鸣器，以提醒佩戴者及周边人员注意。系统不提供专门的警报查看页面，警报信息也不通过页面展示。

== 警报类型详解

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*类型*], [*触发条件*], [*说明*],
  [距离警报], [两个设备间距离小于安全距离], [设备距离过近，存在碰撞风险时触发],
  [围栏进入警报], [设备进入电子围栏区域], [设备进入不应进入的危险区域时触发],
  [围栏离开警报], [设备离开电子围栏区域], [设备离开安全区域（如指定工作区）时触发],
)

== 警报触发与下发流程

1. 系统检测到满足触发条件（参考上表）。
2. 系统生成警报指令，通过 MQTT 协议推送给目标电子标签。
3. 电子标签接收指令后，立即启动震动和蜂鸣器，持续提醒佩戴者及周边人员。
4. 警报解除条件满足后，系统再次通过 MQTT 下发停止指令，标签停止震动和蜂鸣。

== 警报限流机制说明

系统内置智能限流机制，防止警报风暴：

- *频率限制*：每个设备最多每秒触发 2 次警报（超过频率的警报将被丢弃）。
- *合并机制*：相同状态的警报会被合并（例如持续在围栏内，只触发一次进入警报）。
- *升级机制*：持续未解除的警报，30 分钟后系统会提高提醒强度（如增加蜂鸣频率）。
- *静默时段*：可配置夜间静默时段（如 23:00-06:00），期间仅记录警报事件，不下发震动蜂鸣指令。

== 距离监控与安全设置

=== 设备对距离配置

设置特定设备对之间的安全距离阈值。

*操作步骤：*

1. 进入"电子标记"→"距离设置"页面
2. 在"点对点距离设置"区域，选择第一个设备
3. 选择第二个设备
4. 输入安全距离值（单位：米）
5. 点击"设置距离"按钮

#figure(image("/assets/image-11.png"), caption: [设备对距离配置])

=== 批量距离设置

提供多种批量配置方式，提高配置效率：

#figure(image("/assets/image-12.png"), caption: [批量距离设置界面]) <批量距离设置界面>

*方式1：组合设置*

1. 在"组合距离设置"区域，勾选多个设备（至少2个）
2. 输入统一安全距离
3. 点击"批量设置距离"
4. 系统为所有选中设备对设置相同距离

#figure(image("/assets/image-14.png"), caption: [组合距离 效果示意图])

*方式2：笛卡尔积设置*

1. 在"分组距离设置"区域，选择第一组设备（如所有"人员"类型）
2. 选择第二组设备（如所有"吊车"类型）
3. 输入安全距离
4. 点击"批量设置距离"
5. 系统为第一组与第二组之间的所有组合设置距离

#figure(image("/assets/image-13.png"), caption: [笛卡尔积 效果示意图])

*方式3：类型默认距离*

1. 在"类型管理"中编辑设备类型
2. 设置"默认危险半径"
3. 该类型设备自动应用此默认值

== 电子围栏管理

=== 围栏创建完整流程

*步骤1：进入围栏创建*

1. 进入"地图"→"地图设置"→"电子围栏"
2. 点击"创建围栏"按钮
3. 进入围栏绘制页面

#figure(image("assets/围栏属性设置界面.png"), caption: [围栏属性设置界面]) <围栏属性设置界面>

*步骤2：选择围栏类型*

1. 选择"室内围栏"或"室外围栏"
2. 室内围栏关联UWB地图，室外围栏关联RTK地图

*步骤3：输入基本信息*

1. 输入围栏名称（必填，如"高压危险区"）
2. 输入描述（可选，如"110kV变压器周边10米范围"）

*步骤4：绘制围栏边界*

1. 在地图上点击，放置第一个顶点
2. 移动鼠标，点击放置第二个顶点
3. 继续点击，添加更多顶点（至少3个，形成多边形）
4. 双击或点击"完成绘制"，闭合多边形

#figure(image("assets/电子围栏创建界面.png"), caption: [电子围栏绘制界面]) <电子围栏创建界面>

*绘制技巧：*
- 按住Shift键可绘制45度角直线
- 点击已放置顶点可拖动调整位置
- 点击顶点按Delete键可删除

*步骤5：调整顶点坐标（精确编辑）*

1. 在"多边形顶点"列表中，显示所有顶点坐标
2. 直接输入X/Y数值，精确调整位置
3. 点击"删除"按钮移除顶点

#figure(image("/assets/image-15.png"), caption: [围栏顶点精确编辑])

*步骤6：保存围栏*

1. 确认围栏形状和属性正确
2. 点击"完成并保存"按钮
3. 系统验证围栏数据（检查自相交等）
4. 保存成功，围栏立即生效

=== 围栏检测机制

系统实时检测设备与围栏的空间关系：

- *检测频率*：设备位置更新时实时计算（通常1秒1次）
- *判定算法*：射线法（Ray Casting）判断点是否在多边形内
- *状态缓存*：记录设备上一时刻的围栏状态，避免重复触发
- *延迟阈值*：连续3次检测状态一致才触发警报（防止抖动）

== 基站管理

=== 基站配置

管理UWB定位基站信息。

#figure(image("assets/UWB基站配置界面.png"), caption: [UWB基站配置界面])

*基站参数说明：*

#table(
  columns: (auto, 1fr),
  align: left,
  [*参数*], [*说明*],
  [基站名称], [显示名称，如"A基站"、"东北角基站"],
  [基站ID], [唯一标识，通常与硬件ID对应],
  [X坐标], [在UWB坐标系中的水平位置（单位：厘米）],
  [Y坐标], [在UWB坐标系中的垂直位置（单位：厘米）],
  [Z坐标], [高度位置（可选，多层建筑使用）],
  [状态], [在线/离线/故障],
)

=== 添加基站流程

*步骤1：打开添加对话框*

1. 在基站管理页面，点击"添加基站"按钮

*步骤2：填写信息*

1. 输入基站名称
2. 输入X、Y坐标（根据实际测量或配置）
3. 选择基站类型（主基站/从基站）

*步骤3：保存*

1. 点击"创建"按钮
2. 基站立即在UWB地图上显示
3. 系统开始接收该基站数据

=== 基站可视化

- 基站以蓝色三角形标记显示在UWB地图上
- 标记旁显示基站名称
- 在线基站标记为实心，离线为空心
- 点击标记显示基站详情（坐标、最后通信时间）

== 快捷操作汇总

#table(
  columns: (auto, 1fr, 1fr),
  align: left,
  [*操作*], [*触发方式*], [*说明*],
  [查看设备详情], [点击地图上的设备标记], [弹出信息卡片，显示实时状态],
  [缩放地图], [鼠标滚轮/地图控制按钮], [放大查看细节，缩小查看全局],
  [移动地图], [按住鼠标拖动], [平移查看不同区域],
  [刷新数据], [按F5或点击刷新按钮], [重新加载最新数据],
  [切换主题], [点击右上角太阳/月亮图标], [切换浅色/深色模式],
  [全屏显示], [双击地图区域], [进入全屏模式，隐藏其他面板],
  [快速搜索], [按Ctrl+K], [打开全局搜索，快速定位设备],
)

== 标签与类型管理

=== 标签管理

标签用于对设备进行多维度标记，支持灵活分组。

*创建标签：*
1. 创建设备时，在"分组"输入框输入新标签名称
2. 按Enter键，系统自动创建该标签
3. 标签创建后可在其他设备上复用

*查看同标签设备：*
1. 在设备列表中，点击设备的分组标签
2. 系统自动筛选显示所有带该标签的设备

#figure(image("assets/标签相同的设备列表.png"), caption: [标签相同的设备列表])

=== 类型管理

设备类型定义分类和默认安全参数。

*创建类型：*

1. 进入"电子标记"→"标记管理"→"类型管理"
2. 点击"添加类型"按钮
3. 输入类型名称（如"挖掘机"、"安全帽"）
4. 设置默认危险半径（可选）
5. 点击"创建"

#figure(image("assets/创建设备的类型.png"), caption: [创建设备的类型]) <创建设备的类型>

*类型应用：*

1. 创建设备时选择类型，自动继承默认危险半径
2. 修改类型默认半径，不影响已创建设备（可手动同步）
3. 类型用于快速筛选和批量设置

#figure(image("assets/按标签筛选的设备列表.png"), caption: [按标签筛选的设备列表])

= 附录

== 术语表

#table(
  columns: (auto, 1fr),
  align: left,
  [*术语*], [*说明*],
  [RTK], [Real-Time Kinematic，实时动态载波相位差分技术，高精度GPS定位，厘米级精度，用于室外],
  [UWB], [Ultra-Wideband，超宽带定位技术，分米级精度，适用于室内环境],
  [MQTT], [Message Queuing Telemetry Transport，物联网消息传输协议，轻量级发布/订阅模式],
  [电子围栏], [Geofencing，地图上划定的虚拟边界区域，用于区域管控],
  [轨迹], [Trajectory，设备历史移动路径，按时间序列记录的位置点集合],
  [PostgreSQL], [开源关系型数据库，存储结构化数据（设备信息、围栏配置等）],
  [PostGIS], [PostgreSQL空间扩展，支持地理空间数据存储和计算（多边形、坐标等）],
  [MongoDB], [开源文档数据库，存储轨迹等非结构化时序数据],
  [JWT], [JSON Web Token，用于用户认证的令牌机制],
  [WebSocket], [全双工通信协议，用于实时数据推送],
  [微服务], [Microservices，将系统拆分为多个独立部署的服务单元],
  [API网关], [API Gateway，统一入口，负责路由、认证、限流],
  [射线法], [Ray Casting Algorithm，判断点是否在多边形内的经典算法],
  [Haversine公式], [计算球面两点间距离的公式，用于RTK距离计算],
)

== 联系方式

如有问题，请联系系统管理员

== 版本历史

#table(
  columns: (auto, auto, 1fr),
  align: center,
  [*版本*], [*日期*], [*更新内容*],
  [V1.0.0], [2025-12-22], [初始版本发布，包含实时地图、历史轨迹、设备管理、警报查看、电子围栏、距离监控等核心功能],
)

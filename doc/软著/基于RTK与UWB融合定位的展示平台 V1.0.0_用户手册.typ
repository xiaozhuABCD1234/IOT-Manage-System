// ---------- 可调参数 ----------
#let soft-name = "基于RTK与UWB融合定位的展示平台"
#let soft-ver = "V1.0.0"
#let doc-type = "用户手册"

#set heading(numbering: "1.1.1")

// ---------- 页眉函数 ----------
#let soft-header() = context {
  set text(size: 10.5pt, font: ("SimSun", "Songti SC", "serif"))
  smallcaps(soft-name) + h(1fr) + str(counter(page).at(here()).first())
  v(2pt, weak: true)
  line(length: 100%, stroke: 0.25pt)
}

// ---------- 封面 ----------
#let cover() = {
  align(center)[
    #text(size: 20pt, weight: "bold", soft-name + " " + soft-ver) \
    #v(3em)
    #place(center + horizon, text(size: 20pt, doc-type))
  ]
  pagebreak()
}

// ---------- 全局设置 ----------
#set page(
  margin: (top: 2.8cm, bottom: 2.5cm, x: 2cm),
  header: soft-header(),
  numbering: none,
)
#set par(justify: true)
#cover()

#outline(title: "目录")
#pagebreak()
#counter(page).update(1)
#set page(
  margin: (top: 2.8cm, bottom: 2.5cm, x: 2cm),
  header: soft-header(),
  numbering: none,
)

#set text(lang: "zh", region: "cn")
#set figure(numbering: "1")

= 系统概述

== 展示平台简介

展示平台是 IOT 管理系统的监控查看端，面向需要实时监控设备状态、查看位置信息的一线操作人员。

本系统是基于 RTK（实时动态载波相位差分技术）与 UWB（超宽带）融合定位技术的可视化展示平台。通过实时精确的位置跟踪，可以有效监控施工人员进入危险区域、设备碰撞预警、作业边界管控等，显著提升施工安全水平。

== 主要功能

#table(
  columns: (auto, 1fr),
  align: left,
  [*功能*], [*说明*],
  [实时地图], [在地图上查看设备当前位置，支持 RTK（室外）和 UWB（室内）两种定位模式],
  [地图总览], [综合展示所有设备的实时位置],
  [历史轨迹], [查看设备历史移动轨迹],
  [设备状态], [查看设备在线/离线状态],
  [警报查看], [查看设备触发的警报信息],
)

== 系统架构

展示平台采用前后端分离架构，前端基于 Vue 3 + TypeScript 构建，通过 API 网关与后端微服务通信。

#figure(image("assets/image-10.png"), caption: [总体架构设计])
== 技术架构

#table(
  columns: (auto, 1fr),
  align: left,
  [*类别*], [*技术栈*],
  [前端框架], [Vue 3.5.18 + TypeScript 5.8 + Vite],
  [UI组件库], [Radix Vue + Tailwind CSS 4.1],
  [地图服务], [高德地图API + 自定义Canvas绘制],
  [实时通信], [MQTT.js 5.14+（WebSocket）],
  [状态管理], [Pinia 3.0+],
  [路由管理], [Vue Router 4.5+],
  [HTTP客户端], [Axios 1.11+],
)

= 快速开始

== 环境要求

- 现代浏览器（Chrome、Firefox、Edge、Safari）
- 网络连接（需与服务器正常通信）

== 访问系统

1. 打开浏览器
2. 输入展示平台地址：`http://localhost`
3. 进入登录页面

== 登录

1. 输入用户名和密码
2. 点击"登录"按钮
3. 登录成功后自动跳转首页

#figure(
  image("assets/登录界面.png", height: 8cm),
  caption: [登录页面],
) <登录页面>

*图 2-1：展示平台登录页面*

== 默认账户

#table(
  columns: (auto, auto, auto),
  align: center,
  [*角色*], [*用户名*], [*密码*],
  [管理员], [admin], [admin],
)

== 退出登录

1. 点击页面右上角用户名
2. 选择"退出登录"选项

= 界面导航

== 顶部导航菜单

#figure(
  image("/assets/image-3.png"),
  caption: [顶部导航菜单],
) <顶部导航菜单>

*图 3-1：顶部导航菜单*

== 菜单说明

=== 地图菜单

#figure(
  image("assets/地图子菜单.png", height: 7cm),
  caption: [地图子菜单],
) <地图子菜单>

- 地图总览：综合展示所有设备位置
- 实时 RTK 地图：高德地图 + RTK 定位（室外）
- 实时 UWB 地图：自定义地图 + UWB 定位（室内）
- UWB 基站管理：查看基站位置
- 地图设置：查看已配置的地图

=== 电子标记菜单

#figure(
  image("assets/电子标记子菜单.png", height: 7cm),
  caption: [电子标记子菜单],
) <电子标记子菜单>

- 标记状态：设备在线状态监控
- 标记管理：查看设备列表
- 距离设置：查看设备安全距离配置

== 页面结构

#figure(image("assets/页面结构.png"), caption: [页面结构示意图])

= 实时地图

实时地图用于在地图上查看设备的当前位置。

== RTK 实时地图

RTK（实时动态定位）是一种高精度 GPS 定位技术，适用于室外环境。

*功能说明：*
- 在高德地图上显示设备实时位置
- 支持多设备同时显示
- 显示设备最新坐标（经纬度）
- 实时更新设备位置

*访问路径：* 地图 → 实时 RTK 地图

*操作说明：*
1. 进入 RTK 实时地图页面
2. 地图加载后显示当前区域
3. 在线设备以标记形式显示在地图上
4. 标记颜色表示不同状态：
  - 绿色：设备在线，正常
  - 红色：设备触发警报
5. 点击设备标记可查看详细信息

#figure(
  image("assets/室外RTK地图.png"),
  caption: [RTK实时地图页面],
) <室外RTK地图>


== UWB 实时地图

UWB（超宽带）是一种室内定位技术，适用于室内环境。

*功能说明：*
- 在自定义地图（平面图）上显示设备位置
- 室内定位精度高
- 支持多设备同时显示
- 实时更新设备位置

*访问路径：* 地图 → 实时 UWB 地图

*操作说明：*
1. 进入 UWB 实时地图页面
2. 系统加载已配置的自定义地图（平面图）
3. 设备以坐标点形式显示在地图上
4. 基站位置也会显示在地图上
5. 点击设备标记可查看详细信息

#figure(
  image("assets/室内UWB地图.png"),
  caption: [UWB实时地图页面],
) <室内UWB地图>


== 地图总览

综合展示页面，可同时查看 RTK 和 UWB 设备的实时位置。

*访问路径：* 地图 → 地图总览

*功能说明：*
- 同时显示室内外所有设备
- 快速了解整体监控状态
- 适合指挥调度场景

#figure(
  image("/assets/image-4.png"),
  caption: [地图总览页面],
) <地图总览>


= 历史轨迹

查看设备历史移动轨迹，用于回放设备运动路径。

== 功能说明

- 显示设备历史移动路径
- 支持时间范围筛选
- 支持多设备轨迹对比
- 轨迹数据存储在 MongoDB 中

== 查看轨迹

*访问路径：* 地图 → 实时 RTK 地图 / 实时 UWB 地图

*操作步骤：*
1. 进入地图页面
2. 在设备列表或设备选择器中选择要查看的设备
3. 选择时间范围
4. 系统显示该设备的移动轨迹
5. 轨迹以线段或点序列形式呈现

== 轨迹显示说明

#table(
  columns: (auto, 1fr),
  align: left,
  [*元素*], [*说明*],
  [轨迹线], [连接设备历史位置的线段],
  [轨迹点], [设备经过的位置标记],
)

= 设备状态监控

设备状态监控页面用于查看所有设备的在线/离线状态。

== 标记状态

*访问路径：* 电子标记 → 标记状态

*显示信息：*

#table(
  columns: (auto, 1fr),
  align: left,
  [*字段*], [*说明*],
  [设备名称], [设备显示名称],
  [设备类型], [设备所属分类],
  [在线状态], [在线/离线],
  [最后在线时间], [最近一次收到数据的时间],
  [最后位置], [设备最后已知位置],
)

#figure(
  image("assets/设备状态监控.png"),
  caption: [设备标记状态页面],
) <设备状态监控>

== 状态说明

#table(
  columns: (auto, 1fr, auto),
  align: left,
  [*状态*], [*说明*], [*标识*],
  [在线], [设备当前正在发送数据], [绿色],
  [离线], [设备超过一定时间未发送数据], [灰色/红色],
)

== 筛选功能

支持按以下条件筛选设备：

- 设备类型：按设备分类筛选
- 设备标签：按标签筛选
- 在线状态：只看在线/离线设备

== 状态刷新

- 页面自动定时刷新设备状态
- 也支持手动刷新
- 实时数据通过 MQTT 推送更新

== 设备标记管理

提供设备的 CRUD（创建、读取、更新、删除）功能：

#figure(
  image("assets/设备标记管理.png"),
  caption: [设备标记管理],
) <设备标记管理>

1. 创建设备：输入设备信息、选择类型和标签
2. 设备列表：分页展示所有设备，支持搜索和筛选
3. 设备编辑：修改设备基本信息
4. 设备删除：移除不需要的设备记录

#grid(
  columns: (1fr, 1fr),
  gutter: 2em,

  [
    #figure(
      image("assets/创建标记.png", height: 10cm),
      caption: [创建设备],
    ) <创建设备>
  ],

  [
    #figure(
      image("assets/修改标记.png", height: 10cm),
      caption: [修改设备],
    ) <修改设备>
  ],
)

= 警报查看

警报页面用于查看设备触发的各类警报信息。

== 警报类型

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*类型*], [*触发条件*], [*说明*],
  [距离警报], [两个设备间距离小于安全距离], [设备距离过近时触发],
  [围栏进入警报], [设备进入电子围栏区域], [不应进入的区域],
  [围栏离开警报], [设备离开电子围栏区域], [离开安全区域],
)

== 警报显示

*访问路径：* 导航菜单中的警报入口

*显示信息：*

#table(
  columns: (auto, 1fr),
  align: left,
  [*字段*], [*说明*],
  [警报时间], [警报触发的时间],
  [警报类型], [距离/围栏进入/围栏离开],
  [涉及设备], [触发警报的设备],
  [警报详情], [具体信息，如距离值、围栏名称等],
  [处理状态], [未处理/已处理],
)

#figure(
  image("assets/黑夜模式示例.png"),
  caption: [警报查看页面],
) <警报查看>


== 警报颜色标识

#table(
  columns: (auto, auto, 1fr),
  align: left,
  [*状态*], [*颜色*], [*说明*],
  [距离警报], [橙色/黄色], [设备距离过近],
  [围栏进入警报], [红色], [设备进入禁区],
  [围栏离开警报], [蓝色], [设备离开安全区],
)

== 警报处理

- 查看警报详情
- 标记警报为已处理
- 持续未处理的警报可能持续显示

== 警报说明

系统内置警报限流机制，防止警报风暴：

- 每个设备最多每秒 2 次警报
- 相同状态的警报会被合并
- 避免大量重复警报干扰

== 距离监控与安全设置

=== 设备对距离配置

设置特定设备对之间的安全距离阈值，当设备间距离超过阈值时触发警报。

=== 批量距离设置

提供多种批量配置方式：

#figure(
  image("assets/批量距离设置界面.png"),
  caption: [批量距离设置界面],
) <批量距离设置界面>

1. 组合设置：选择多个设备，统一设置安全距离
2. 笛卡尔积设置：为两组设备间的所有组合设置相同距离
3. 类型默认距离：设置同类型设备间的默认安全距离

== 电子围栏管理

=== 围栏创建

1. 绘制围栏：在地图上点击绘制多边形围栏
2. 围栏属性：设置围栏名称、描述、激活状态
3. 围栏类型：区分室内围栏和室外围栏

#figure(
  image("assets/围栏属性设置界面.png"),
  caption: [围栏属性设置界面],
) <围栏属性设置界面>

#figure(
  image("assets/电子围栏创建界面.png"),
  caption: [电子围栏绘制界面],
) <电子围栏创建界面>

=== 围栏检测

系统实时检测设备是否进入或离开电子围栏：

1. 实时检测：设备位置更新时自动检测围栏状态
2. 警报触发：设备进入围栏时触发警报
3. 状态缓存：避免重复警报，智能限流

== 基站管理

=== 基站配置

管理 UWB 定位基站信息：

#figure(
  image("assets/UWB基站配置界面.png"),
  caption: [UWB基站配置界面],
)

1. 基站添加：设置基站名称、坐标位置
2. 基站列表：展示所有基站信息
3. 基站编辑：修改基站配置
4. 基站删除：移除不需要的基站

=== 基站可视化

在地图上显示基站位置，支持基站状态监控。


== 快捷操作

#table(
  columns: (auto, 1fr),
  align: left,
  [*操作*], [*说明*],
  [点击设备标记], [查看设备详情],
  [缩放地图], [查看不同范围的监控区域],
  [刷新页面], [获取最新数据],
)

== 标签与类型管理

=== 标签管理

- 标签创建：设备的标签创建在创建设备时一起创建
- 列出标签相同的设备

#figure(
  image("assets/标签相同的设备列表.png"),
  caption: [标签相同的设备列表],
)

=== 类型管理

管理设备类型，定义类型间的默认安全距离

#figure(
  image("assets/创建设备的类型.png"),
  caption: [创建设备的类型],
) <创建设备的类型>

#figure(
  image("assets/按标签筛选的设备列表.png"),
  caption: [按标签筛选的设备列表],
)


= 附录

== 术语表

#table(
  columns: (auto, 1fr),
  align: left,
  [*术语*], [*说明*],
  [RTK], [实时动态定位，高精度 GPS 定位技术，用于室外],
  [UWB], [超宽带定位技术，适用于室内环境],
  [MQTT], [物联网消息传输协议，用于设备数据推送],
  [电子围栏], [地图上划定的虚拟边界区域],
  [轨迹], [设备历史移动路径],
  [PostgreSQL], [关系型数据库，存储结构化数据],
  [PostGIS], [PostgreSQL空间扩展，支持地理空间数据],
  [MongoDB], [文档数据库，存储轨迹等非结构化数据],
)
== 联系方式

如有问题，请联系系统管理员。

== 版本历史

#table(
  columns: (auto, auto, 1fr),
  align: center,
  [*版本*], [*日期*], [*更新内容*],
  [V1.0.0], [2025-12-22], [初始版本发布，包含基本定位展示和设备管理功能],
)

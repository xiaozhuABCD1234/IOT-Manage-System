on: push

jobs:
    npm-build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: |
                cd frontend
                npm install
                npm run build